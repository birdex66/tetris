#include <iostream>
#include <stdlib.h>
#include <time.h>

using namespace std;

int main(){
    srand(time(0));
    //min = 1
    //max = 10
    string input;
    while(true){
//        cin >> input;
        cout << (rand() % (10-1+1)) + 1 << endl;
    }
}
